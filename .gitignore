# ── 关于 lib/client.js ──
# 提交「干净版」：node build.cjs --clean 生成（只含下方已提交素材）。
# 本地跑完整构建 node build.cjs（含第三方壁纸）会变大，请勿误提交完整版。

# ── 依赖与系统文件 ──
node_modules/
.DS_Store
Thumbs.db

# 构建产物：node build.cjs 生成，不提交（prepack 自动构建）
lib/client.js
node_modules.orig/

# ── 运行/临时文件（启停脚本 PID、预览日志、回收残留）──
*.pid
*.log
*.trash*
*.bak*


# ── npm 打包产物（pnpm pack 生成，随 GitHub Release 分发）──
*.tgz


audit-history